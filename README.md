# 4-gridRenderClasses

## Organize project into classes based on requirements and UML diagram, as well as update grid.

- Implement a Camera class based on a given UML diagram using the provided method stubs to manage all camera render code.
- Refine Main class by adding a renderer class and limiting Main to only call the starting render method.
- Create a spot file to house all universally accessible variables.

Also played around with adding some code from 3, changing the grid from one color to show a different color based on whether
or not the grid cell is alive. However, all necessary files aren't included in this repo, only the files for the changes
listed above are included; there's just some leftover testing code in the renderer class.
